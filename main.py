from train import run_training
from train import test_existing_model, impute_missing_values, train_on_imputed

# run_training(n_epochs=10000)
# impute_missing_values()
train_on_imputed()

# test_existing_model()